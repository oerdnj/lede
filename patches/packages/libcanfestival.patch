--- packages.orig/libs/libcanfestival/Makefile
+++ packages/libs/libcanfestival/Makefile
@@ -49,6 +49,7 @@ CONFIGURE_ARGS := \
 	--binutils=$(TARGET_CROSS) \
 	--os=Linux \
 	--kerneldir=$(LINUX_DIR) \
+	--binutils=$(GNU_TARGET_NAME)- \
 	--prefix=/usr \
 	--target=unix \
 	--can=socket \
