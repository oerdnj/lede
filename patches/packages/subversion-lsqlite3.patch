--- packages.orig/net/subversion/Makefile
+++ packages/net/subversion/Makefile
@@ -102,7 +102,7 @@ endif
 
 CONFIGURE_VARS += \
 	LDFLAGS="$(TARGET_LDFLAGS) -lcrypt -lm \
-		-lz -lpthread $(if $(INTL_FULL),-lintl)"
+		-lz -lsqlite3 -lpthread $(if $(INTL_FULL),-lintl)"
 	CPPFLAGS="$(TARGET_CPPFLAGS)"
 
 define Build/Compile
